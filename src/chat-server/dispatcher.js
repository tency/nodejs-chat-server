const log = logger.getLogger("dispatcher");

module.exports = class Dispatcher {
    constructor() {
        log.debug("Dispatcher constructor");
    }

    // 订阅消息
    subscribeMessage() {

    }

    // 处理消息
    handleMessage(conID, data, callback) {

    }
}